#! /bin/bash

buildDriver()
{
        echo
        if (make)
        then
                echo "Driver build successfully..."
        else
                echo "Driver build failed !!!"
        fi
}

